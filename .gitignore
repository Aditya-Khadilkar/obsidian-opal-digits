# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Playwright
test-results
playwright-report
blob-report

# Cloudflare / wrangler
.wrangler

# Reference imagery. The source photograph is 4 MB and is the author's own; the
# repo records what was measured from it instead. Drop it in locally to re-run
# the analysis or the side-by-side comparisons.
reference/*.png
reference/*.jpg
reference/*.jpeg

# Captured screenshots. Build artifacts of the Playwright harness, several
# megabytes each; regenerate with `npm run shots`.
screenshots/**/*.png
